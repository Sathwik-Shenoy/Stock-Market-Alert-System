{
  "name": "stock-market-alert-system",
  "version": "1.0.0",
  "description": "MERN Stack Stock Market Alert System with real-time notifications",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "keywords": [
    "stock",
    "alerts",
    "mern",
    "trading",
    "notifications"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.5"
  }
}
